class Photo {
  int? index;
  int? albumid;
  int id;
  String title;
  String url;
  String thumbnailUrl;

  //constructor
  Photo(this.index, this.albumid, this.id, this.title, this.url,
      this.thumbnailUrl);
}
